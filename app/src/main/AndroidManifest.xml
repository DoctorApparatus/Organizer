<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cyberstuck.organizer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_calendar"></activity>
        <activity android:name=".calendar" />
        <activity
            android:name=".Activity_landing_page"
            android:label="@string/title_activity_landing_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_landing" />
        <activity
            android:name=".things_to_pay"
            android:label="@string/title_activity_things_to_pay"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".places_to_be"
            android:label="@string/title_activity_places_to_be"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>